---
interface Props {
  position: 'header' | 'in-content' | 'footer';
}

const { position } = Astro.props;
---

<!-- Auto ads handles placement via the AdSense script in BaseHead.astro -->
<!-- This component reserves semantic positions for future manual ad units if needed -->
